{
  "name": "yusan",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@tweakpane/plugin-essentials": "^0.2.1",
    "gh-pages": "^6.3.0",
    "tweakpane": "^4.0.3",
    "vite": "^5.2.0"
  }
}
